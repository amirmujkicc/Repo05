function main() {
    // Only change code below this line
    var myFirstString = "I think therefore I \"am\" -- sentient."
    var mySecondStrin = "No, you only \"think\", so you aren't."
    // Only change code above this line

    return {
        myFirstString,
        mySecondStrin
    };
}

console.log(main());
module.exports = main;